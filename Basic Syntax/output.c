#include <stdio.h>
int main() {
    int rahim, karim;
    char K;
    float f;
    scanf("%d %c %f", &rahim, &K , &f);
    printf("%d %c %0.2f", rahim, K, f);

    int l,o;
    scanf("%d%% %d%%", &l, &o);
    printf("%d%% %d%%", l,o);
    return 0;
}